// Created by Liam on 2022-09-13.

#include <cstdio>
#include <iostream>
using namespace std;

#include <string>

int sum(int x, int y)
{
    int sum;
    sum = x + y;
    return sum;
}

int main(){

/*    int arr[20];
    int *j;

    arr[0] = 0;
    arr[1] = 2;
    arr[2] = 4;
    arr[3] = 6;

    j = arr; // Same as j = &arr[0]
    j = j + 1;

    //printf("%d\n",*j);

    int y;

    y = 10;

    j = &y;

    //printf("%d\n",*j);

    // enum

    enum DAY{
        MON = 1,TUE,WED,THU,FRI,SAT,SUN
    };

    DAY day_tracker;
    day_tracker = MON;
    printf("%d\n",day_tracker+2);

    // typedef

    enum color {
        RED = 5,GREEN = 10,BLUE = 15
    };

    color red;
    red = RED;

    printf("%d\n",red);*/

/*// Structures

    struct birthday{

        int year;
        int month;
        int day;

        int hour;
        int minute;
        int am_or_pm;

    };

    birthday liam;

    cout << "Enter your birthday information!" << endl;

    int year;
    cout << "Enter your year of birth" << endl;
    cin >> year;
    liam.year = year;

    int month;
    cout << "Enter the month" << endl;
    cin >> month;
    liam.month = month;

    int day;
    cout << "Enter the day" << endl;
    cin >> day;
    liam.day = day;

    int hour;
    cout << "Enter the hour" << endl;
    cin >> hour;
    liam.hour = hour;

    int minute;
    cout << "Enter the minute" << endl;
    cin >> minute;
    liam.minute = minute;

    int am_or_pm;
    cout << "Enter if 0 if am or 1 if pm" << endl;
    cin >> am_or_pm;
    liam.am_or_pm = am_or_pm;

    cout << "Would you like to see what day of the week your were born?" << endl;

    string response;

    cin >> response;

    if (response == "Yes"){
        cout << "Fuck you retard you think ik how to do that lmao" << endl;
    }
    else{
        cout << "Have a nice day!" << endl;
    }*/

/*
    int a, b, ans;

    a = 4;
    b = 5;

    ans = sum(a,b);

    cout << ans;
*/

}